spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Quang292001/git-config-repo.git
          clone-on-start: true
        bootstrap: true
      enabled: true

  application:
    name: Config-Server